{% extends 'base-front.html.twig' %}

{% block title %}{{ parent() }}Changement avatar{% endblock %}

{% block body %}
    <div class="container">
        <h1>Veuillez choisir un avatar.</h1>
        <div class="row">
            <div class="col-5">
                {{form_start(form)}}
                    {{form_row(form.avatar)}}
                    <input type="submit">
                {{form_end(form)}}
            </div>
        </div>
    </div>
{% endblock %}
