{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}/Editeurs{% endblock %}

{% block body %}

<div class="container">
    <h2>Les éditeurs</h2>
    <table class="table table-dark table-hover table-striped border border-light border-2">
        <thead>
            <tr>
                <th scope="col col-2">Pays</th>
                <th scope="col col-3">Nom</th>
                <th scope="col col-3">Site Web</th>
                <th scope="col col-1">Nb jeux</th>
            </tr>
        </thead>
        <tbody>
            {% for publisher in publishers %}
            <tr>
                <th scope="row"><img src="{{ publisher.country.urlFlag }}"></th>
                <td>{{ publisher.name }}</td>
                <td><a href="{{ publisher.website }}" target="_blank">{{ publisher.website }}</a></td>
                <td>{{ publisher.games|length }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}